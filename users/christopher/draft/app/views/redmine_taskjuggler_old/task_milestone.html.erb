			task t<%= issue.id %> "#<%= issue.id %> : <%= issue.subject.sub("\"","'") %>" {
				milestone
<%= 
				render :file  => File.join(File.dirname(__FILE__),"inc_task_inc_depends.html.erb"), :locals => { :issue => issue } %>
			}