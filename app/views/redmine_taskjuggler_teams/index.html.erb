<h2>TjTeamsController#index</h2>

<% @tj_teams.each do |team| %>

  <p>
    <%= link_to team.name, redmine_taskjuggler_team_path(team) %>
  </p>

<% end %>

Relative Root : <%=Redmine::Utils.relative_url_root %>

<%= form_tag("/redmine_taskjuggler_teams/new", method: "post") do %>
  <%= label_tag(:tj_team_name, "New team :") %>
  <%= text_field_tag(:tj_team_name) %>
  <%= submit_tag("New") %>
<% end %>

Plugin settings are <a href="/settings/plugin/redmine_taskjuggler">here</a>
